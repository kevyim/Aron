{
  "mcpServers": {
    "higgsfield": {
      "command": "python3",
      "args": ["-m", "higgsfield_mcp.server"],
      "env": {
        "HF_API_KEY": "${HF_API_KEY}",
        "HF_SECRET": "${HF_SECRET}"
      }
    }
  }
}
